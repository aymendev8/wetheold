{% extends 'base.html.twig' %}

{% block title %} profil - {{app.user.pseudo}}{% endblock %}

{% block body %}
    <ul class="nav nav-tabs" role="tablist">
        <li class="nav-item" role="presentation">
            <a class="nav-link" data-bs-toggle="tab" href="{{ path('user.profil') }}" aria-selected="false" role="tab">Mes infos </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" data-bs-toggle="tab" href="{{ path('user.commande') }}" aria-selected="false" role="tab">Mes commandes </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" data-bs-toggle="tab" href="#messages" aria-selected="false" role="tab">Carnet d'adresse </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" data-bs-toggle="tab" href="#settings" aria-selected="false" role="tab">Nous contacter </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link active" data-bs-toggle="tab" href="{{ path('user.password') }}" aria-selected="true" role="tab">Changer mot de passe </a>
        </li>
        <li class="nav-item" role="presentation">
            <a class="nav-link" data-bs-toggle="tab" href="{{ path("security.logout") }}" aria-selected="false" role="tab"> Déconnexion </a>
        </li>
    </ul>

    {% for message in app.flashes('danger') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}

    <div class="container">
        {{ form(form) }}
    </div>
{% endblock %}
